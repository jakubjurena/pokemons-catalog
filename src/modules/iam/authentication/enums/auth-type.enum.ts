/**
 * AuthType
 * @description An enum for the different types of authentication.
 * @property Bearer - Bearer token authentication
 * @property None - No authentication
 */
export enum AuthType {
  Bearer,
  None,
}
